import { ThaiDateAbbrPipe } from './thai-date-abbr.pipe';

describe('ThaiDateAbbrPipe', () => {
  it('create an instance', () => {
    const pipe = new ThaiDateAbbrPipe();
    expect(pipe).toBeTruthy();
  });
});
